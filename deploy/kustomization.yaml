apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml

images:
  - name: ghcr.io/settonra/dubtracker
    newTag: latest # bump to a :sha-... tag to pin; CI publishes both
